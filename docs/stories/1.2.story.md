# Story 1.2: Hamburger menu mobile

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["manual review", "browser testing", "responsive check"]

## Story

**As a** visitante mobile,
**I want** acceder a la navegación del sitio,
**so that** pueda saltar a cualquier sección sin hacer scroll largo.

## Acceptance Criteria

1. En viewport < 480px aparece un icono hamburger en el nav
2. Al tocar se despliega menú con: Experiencias, Carta, Turismo, Reservar
3. Al seleccionar una opción el menú se cierra y hace scroll suave a la sección
4. Animación de apertura/cierre suave, coherente con el estilo premium del sitio
5. Implementado en vanilla JS (sin librería)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Agregar HTML del hamburger button y mobile menu (AC: 1, 2)
  - [x] Insertar `<button class="nav-hamburger">` dentro de `<nav>`, después de `.nav-brand`
  - [x] Insertar `<div class="nav-mobile-menu">` con 4 links: Experiencias, Carta, Turismo, Reservar
  - [x] Agregar atributos ARIA: `aria-label="Abrir menú"`, `aria-expanded="false"`, `role="dialog"`
- [x] Task 2: Agregar CSS del hamburger y mobile menu (AC: 1, 4)
  - [x] Estilos del botón hamburger: 3 `<span>` como líneas, hidden en desktop
  - [x] Animación de 3 líneas → X cuando `.active` (transición 0.4s `var(--ease)`)
  - [x] Estilos del overlay menu: fullscreen, `var(--bg-deep)` opacity 0.95, links centrados
  - [x] Tipografía grande para links mobile (`var(--heading-font)`)
  - [x] CSS en sección Nav (~línea 153) y responsive breakpoint < 480px (~línea 522)
- [x] Task 3: Agregar JS módulo Hamburger Menu — IIFE (AC: 2, 3, 5)
  - [x] Toggle `.active` en button y menu al click
  - [x] Toggle `aria-expanded` true/false
  - [x] Cerrar al click en cualquier link (con smooth scroll)
  - [x] Cerrar al click fuera del menú
  - [x] Cerrar con tecla Escape
  - [x] Prevenir body scroll cuando menú está abierto (`overflow: hidden`)
- [x] Task 4: Verificar responsive (AC: 1, 4)
  - [x] Desktop (960px+): hamburger hidden, nav links visibles
  - [x] Tablet (481-960px): hamburger hidden, nav links visibles
  - [x] Mobile (<480px): hamburger visible, nav links hidden
- [x] Task 5: Verificar animación y UX (AC: 3, 4)
  - [x] Transición suave apertura/cierre
  - [x] Scroll suave al seleccionar sección
  - [x] Sin errores en Console

## Dev Notes

### Archivo a modificar

- **Único archivo:** `outputs/kamba-hopo-lp/index.html`

### Spec de implementación (Architecture §7.6)

**HTML** (dentro de `<nav>`, después de `.nav-brand`):
```html
<button class="nav-hamburger" id="navHamburger"
        aria-label="Abrir menú" aria-expanded="false">
  <span></span><span></span><span></span>
</button>
<div class="nav-mobile-menu" id="navMobileMenu"
     role="dialog" aria-label="Menú de navegación">
  <a href="#experiencias">Experiencias</a>
  <a href="#carta">Carta</a>
  <a href="#turismo">Turismo</a>
  <a href="#cta">Reservar</a>
</div>
```

**CSS locations:**
- Estilos del hamburger y menu: insertar en sección Nav (~línea 153-196)
- Breakpoint rules: insertar en sección Responsive (~línea 522-545, dentro de `@media (max-width: 480px)`)
- Hidden en desktop: `.nav-hamburger { display: none; }` por defecto, `display: block` solo en < 480px

**JS location:**
- Insertar como módulo 8 después de Background Mesh (~línea 919)
- Formato IIFE con guard clause:
```javascript
(() => {
  const hamburger = document.getElementById('navHamburger');
  if (!hamburger) return;
  // Logic here
})();
```

### Design tokens a usar

| Token | Uso |
|-------|-----|
| `var(--bg-deep)` | Background del overlay (con opacity 0.95) |
| `var(--cream)` | Color de las líneas del hamburger |
| `var(--text-0)` | Color de los links del menu |
| `var(--accent)` | Color hover de los links |
| `var(--ease)` | Timing function para transiciones |
| `var(--heading-font)` | Font de los links del menu |

### Coding Standards relevantes

- **Single File Rule:** Todo en `index.html` (Architecture §7.1)
- **IIFE Pattern:** Guard clause, sin shared state (Architecture §5.2)
- **Comment Sections:** `/* ═══ HAMBURGER ═══ */` para CSS nuevo
- **Semantic HTML:** `<button>` para el trigger, ARIA attributes
- **No Dependencies:** Vanilla JS puro (Architecture §7.1)

### Dependencias

- **Story 1.1** debe completarse primero (acentos corregidos antes de agregar funcionalidad)

### Source Tree

```
outputs/kamba-hopo-lp/
└── index.html  ← Modificar: HTML (nav), CSS (nav + responsive), JS (módulo 8)
```

### Testing

- **Framework:** Sin tests automatizados — verificación manual
- **Desktop Chrome (1440px):** Hamburger NO visible, nav links visibles
- **Mobile Chrome (360px):** Hamburger visible, menu funcional, scroll suave
- **Mobile Safari (iPhone):** Igual que Chrome mobile
- **Keyboard:** Escape cierra menú, Tab navega links
- **Console:** Sin errores JS
- **Precaución:** Verificar que cursor custom y magnetic buttons no interfieren

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-28 | 1.0 | Story creada desde PRD v1.1 + Architecture v1.0 | River |
| 2026-02-28 | 1.1 | Validación GO (10/10) — Status Draft → Ready | Pax |
| 2026-02-28 | 1.2 | Implementación completada — hamburger menu mobile | Dex |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (Dex)

### Debug Log References

N/A — sin errores durante implementación

### Completion Notes List

- HTML: `<button class="nav-hamburger">` con 3 spans + `<div class="nav-mobile-menu">` con 4 links insertados en `<nav>`
- CSS: Estilos hamburger (display:none default, display:flex en <480px), animación 3 líneas→X, overlay fullscreen con backdrop-filter blur(20px)
- CSS responsive: `.nav-hamburger { display: flex; }` agregado en `@media (max-width: 480px)`
- JS: Módulo 8 IIFE con toggle, aria-expanded, close on link click, close on Escape, close on outside click, body overflow hidden
- Design tokens usados: --bg-deep, --cream, --text-0, --accent-light, --ease, --heading-font
- No se modificó estructura existente ni otros módulos JS

### File List

- `outputs/kamba-hopo-lp/index.html` — **Modified** (HTML nav, CSS hamburger + responsive, JS módulo 8)

## QA Results

**Verdict: PASS**
**Date:** 2026-03-01 | **Reviewer:** Quinn (@qa)

- AC 1: PASS (hamburger visible < 480px, hidden en desktop)
- AC 2: PASS (4 items: Experiencias, Carta, Turismo, Reservar)
- AC 3: PASS (cierre + smooth scroll)
- AC 4: PASS (animación 0.4s, backdrop-filter, opacity/visibility)
- AC 5: PASS (vanilla JS IIFE, sin dependencias)
- ARIA: PASS (aria-label, aria-expanded, role="dialog")
- Keyboard: PASS (Escape cierra, outside click cierra)
